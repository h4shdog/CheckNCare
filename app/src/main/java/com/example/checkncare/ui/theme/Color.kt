package com.example.checkncare.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary reds ──────────────────────────────────────────────────────────────
val CrimsonRed   = Color(0xFFC0392B)
val DeepRed      = Color(0xFF8B0000)
val BrightRed    = Color(0xFFE53935)
val SoftRed      = Color(0xFFFFCDD2)
val PaleRed      = Color(0xFFFFEBEE)

// ── Light mode neutrals ───────────────────────────────────────────────────────
val PureWhite    = Color(0xFFFFFFFF)
val OffWhite     = Color(0xFFFAFAFA)
val LightGray    = Color(0xFFF5F5F5)
val MidGray      = Color(0xFFBDBDBD)
val DividerGray  = Color(0xFFEEEEEE)

// ── Light mode text ───────────────────────────────────────────────────────────
val TextPrimary  = Color(0xFF1A1A1A)
val TextSecond   = Color(0xFF616161)
val TextHint     = Color(0xFF9E9E9E)

// ── Dark mode surfaces ────────────────────────────────────────────────────────
val DarkBg          = Color(0xFF0F0F0F)   // deepest background
val DarkSurface     = Color(0xFF1A1A1A)   // cards / scaffolds
val DarkSurface2    = Color(0xFF242424)   // elevated cards
val DarkDivider     = Color(0xFF2E2E2E)
val DarkMidGray     = Color(0xFF3A3A3A)

// ── Dark mode text ────────────────────────────────────────────────────────────
val DarkTextPrimary = Color(0xFFF0F0F0)
val DarkTextSecond  = Color(0xFFAAAAAA)
val DarkTextHint    = Color(0xFF666666)

// ── Dark mode reds ────────────────────────────────────────────────────────────
val DarkRed         = Color(0xFFEF5350)   // brighter primary on dark bg
val DarkSoftRed     = Color(0xFF4A1010)   // chip / badge bg in dark mode
val DarkPaleRed     = Color(0xFF2D0A0A)   // info card bg in dark mode

// ── Status ────────────────────────────────────────────────────────────────────
val SuccessGreen = Color(0xFF2E7D32)
val WarnAmber    = Color(0xFFE65100)
val ErrorRed     = Color(0xFFB71C1C)

// ── Gradient stops ────────────────────────────────────────────────────────────
val GradientStart     = Color(0xFFC62828)
val GradientEnd       = Color(0xFF8B0000)
val DarkGradientStart = Color(0xFF1A0000)
val DarkGradientEnd   = Color(0xFF0F0F0F)
